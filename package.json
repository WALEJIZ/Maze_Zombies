{
  "name": "maze-zombies",
  "version": "1.0.0",
  "description": "A Minecraft-style zombie shooter set in a maze. Kill the janitor zombie, grab the key, and escape!",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
